# CLAUDE.md

Claude Code: อ่าน **[AGENTS.md](./AGENTS.md)** เป็นเอกสารหลักของ repo นี้
(ภาพรวมโปรเจกต์ สถานะปัจจุบัน โครงสร้างไฟล์ วิธีรัน และ conventions ทั้งหมดอยู่ในนั้น)

สรุปสั้น: เว็บแอปมือถือแสดงรายการสินค้า/ราคา/สต็อกของร้านของชำ "Fang Fang Shop"
ผู้ใช้ทั่วไปดูอย่างเดียว, พนักงานล็อกอินเพื่อจัดการสต็อก/ราคา/รูป

โค้ดหลักอยู่ที่ `app/page.tsx` — **เชื่อมกับ backend จริงแล้ว** (`../fangfangshop-back`)
ผ่าน `app/api.ts` ทั้งสินค้า ล็อกอิน โปรไฟล์ และคลังรูปกลาง
ส่วน `localStorage` / IndexedDB เหลือหน้าที่แค่ cache ตอนต่อ API ไม่ได้

⚠️ รูปจาก API เป็น path (`/images/...`) ไม่ใช่ URL เต็ม — ต้องผ่าน `resolveImageUrl()` ก่อนใส่ `<img src>`
